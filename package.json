{
  "name": "reliable-webhook-processor",
  "private": true,
  "engines": { "node": ">=24.20.0 <25" },
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspace backend"
  }
}
